# AGENTS.md — FireFly-DayZ-SA (серверные профили)

## Git-доступ
- Репозиторий: `D:\GitHub\FireFly-DayZ-SA` (branch `main`, remote: https://github.com/TheFirstNoob/FireFly-DayZ-SA.git)
- Пользователь предоставил полный доступ: я могу **коммитить и пушить** когда он попросит (проверено 31.07.2026).
- Коммиты идут от имени владельца (`TheFirstNoob`), креды git уже настроены.
- Правила: коммитить только по явной просьбе, только намеренные файлы, стиль сообщений — краткий, по образцу истории (`More quest fixes`, `Quest line fixes and updates`).
- В `D:\My DayZ Mods Workshop` тоже есть git-репозиторий, но там нет коммитов — рабочая папка модов, НЕ коммитить без отдельного запроса.

## Полезные команды
- `git status` — состояние
- `git log --oneline -10` — история для стиля сообщений
- `git push origin main` — пуш после коммита
